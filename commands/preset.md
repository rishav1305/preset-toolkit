---
description: "Preset dashboard management — sync, push, validate, screenshot, visual regression, ownership"
---

Invoke the `preset-toolkit:preset` skill, passing through any arguments the user provided after `/preset`.
